bundle:
  name: sales-analytics

include:
  - resources/**/*.yml

variables:
  catalog_name:
    description: Unity Catalog name
    default: main
  schema_name:
    description: Schema name for tables
    default: sales_analytics

targets:
  dev:
    mode: development
    workspace:
      host: https://your-workspace.cloud.databricks.com
    variables:
      catalog_name: main
      schema_name: dev_sales_analytics

  prod:
    mode: production
    workspace:
      host: https://dbc-3ef25b18-8263.cloud.databricks.com
      profile: prod
      root_path: /Workspace/Users/abhijanjoshi2@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog_name: main
      schema_name: prod_sales_analytics
